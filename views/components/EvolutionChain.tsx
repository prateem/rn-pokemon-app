import React from "react";
import {View, Text, Pressable} from "react-native";
import tw from "twrnc";

export default function EvolutionChain() {
    return (
        <View style={tw`p-8 bg-blue-100`}>
            {
            }
        </View>
    )
}
